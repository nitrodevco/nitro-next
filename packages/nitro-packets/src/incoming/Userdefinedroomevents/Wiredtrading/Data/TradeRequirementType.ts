// Body filled by hand from D:\Habbo\packet-tool\out - the generator has no preserve step, so re-apply after a regeneration.
/** The four constants on Flash `TradeRequirement`, named by the texts `OfferingRequirementsView` shows for them. */
export enum TradeRequirementType {
    /** `_-YP`: any credit furni. */
    AnyCoins = 0,
    /** `_-927`: any furni that is not credit furni. */
    AnyFurni = 1,
    /** `_-Gq`: anything tradeable. */
    AnyAll = 2,
    /** `_-w1m`: what `ITradeRequirement.rules` spells out. There is no 3. */
    Rules = 4,
}
