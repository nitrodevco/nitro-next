// Body filled by hand from D:\Habbo\packet-tool\out - the generator has no preserve step, so re-apply after a regeneration.
/** Flash `TradeRequirementNode.TYPE_COIN` / `TYPE_FURNI`: what one node of a trade rule asks for. */
export enum TradeRequirementNodeType {
    Coin = 0,
    Furni = 1,
}
