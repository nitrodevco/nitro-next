// Body filled by hand from D:\Habbo\packet-tool\out - the generator has no preserve step, so re-apply after a regeneration.
/**
 * The first three constants on the Flash contract contents parser (`_-42y._-02m`), named after the
 * window `WiredContractController` opens for each: `PaymentContract`, `TradeContract`, `RewardContract`.
 */
export enum WiredContractType {
    /** `_-2P` */
    Payment = 0,
    /** `_-x18` */
    Trade = 1,
    /** `_-GU` */
    Reward = 2,
}
