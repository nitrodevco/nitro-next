// Body filled by hand from D:\Habbo\packet-tool\out - the generator has no preserve step, so re-apply after a regeneration.
/**
 * Flash `_-YB._-32X`: what a wired variable is held by - `IWiredVariable.variableTarget`, and the
 * first value of `WiredSetObjectVariableValueComposer`. `FURNI` and `USER` are Flash's names and
 * equal `WiredInputSourcePicker`'s furni and user source; the two negative ones equal
 * `VariableExtraSourceTypes.GLOBAL_SOURCE` and `CONTEXT_SOURCE`.
 */
export enum WiredVariableTarget {
    Furni = 0,
    User = 1,
    /** `_-61B` */
    Global = -10,
    /** `_-O1P` */
    Context = -20,
}
