import { IWiredFurniActionDefBase } from './IWiredFurniActionDefBase';

/** Adds nothing to the base definition in Flash; kept as its own name for the packet that carries it. */
export type IWiredFurniAddonDef = IWiredFurniActionDefBase;
