// Body filled by hand from D:\Habbo\packet-tool\out - the generator has no preserve step, so re-apply after a regeneration.
/**
 * Flash `incoming.userdefinedroomevents._-S1q`, named by `UserDefinedRoomEventsCtrl`, which turns
 * 1, 2 and 3 into the text keys `furni`, `users` and `variables` and treats 0 as "this condition
 * has no quantifier".
 */
export enum QuantifierType {
    /** `_-i1W` */
    None = 0,
    /** `_-91B` */
    Furni = 1,
    /** `_-t1k` */
    Users = 2,
    /** `_-F2R` */
    Variables = 3,
}
