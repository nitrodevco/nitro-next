// Body filled by hand from D:\Habbo\packet-tool\out - the generator has no preserve step, so re-apply after a regeneration.
/** Flash `WiredEnvironment.CLICK_USER_*`: what a click on a user does while the room's wired overrides it. */
export enum WiredClickUserOption {
    Default = 0,
    ClickWalkBehind = 1,
    PassThrough = 2,
}
