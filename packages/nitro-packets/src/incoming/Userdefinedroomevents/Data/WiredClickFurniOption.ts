// Body filled by hand from D:\Habbo\packet-tool\out - the generator has no preserve step, so re-apply after a regeneration.
/** Flash `WiredEnvironment.CLICK_FURNI_*`: what a click on a furni does while the room's wired overrides it. */
export enum WiredClickFurniOption {
    Default = 0,
    PassThrough = 1,
}
