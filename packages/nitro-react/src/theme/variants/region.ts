import { ThemeVariant, ThemeVariants } from '../utils';

/** `Region` variants - the Flash `style` ids it draws. */
export type RegionVariant = ThemeVariant;

export const REGION_VARIANTS: ThemeVariants<RegionVariant> = {
    0: {},
};
