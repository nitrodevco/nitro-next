import { AvatarGenderType } from '@nitrodevco/nitro-api';

type AvatarImageDomProps = {
    figure: string;
    gender: AvatarGenderType;
    headOnly?: boolean;
    direction?: number;
    scale?: number;
};

export const AvatarImagePixi = (props: AvatarImageDomProps) => {

};
