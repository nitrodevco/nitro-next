import { ISimpleRoomObjectData } from '@nitrodevco/nitro-api';

export interface InfostandPetViewProps {
    objectData: ISimpleRoomObjectData;
    onClose: () => void;
}

/** Pixi port of views/room-widgets/object-infostand/InfostandPetView.tsx - DOM's own source is an empty stub, ported at the same stub fidelity. */
export const InfostandPetView = (_props: InfostandPetViewProps) => null;
