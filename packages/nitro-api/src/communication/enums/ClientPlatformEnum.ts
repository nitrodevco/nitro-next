/**
 * The client platform `ClientHelloComposer` reports. Mirrors Flash's obfuscated
 * `com.sulake.habbo.communication.enum.§_-22r§` (paired in `scripts/drift/known.py`).
 */
export enum ClientPlatformEnum {
    Unknown = 0,
    Flash = 1,
    Ios = 2,
    Android = 3,
    WebGl = 4,
    MacOs = 5,
    Win = 6,
    Linux = 7,
}
