/**
 * The device category `ClientHelloComposer` reports. Mirrors Flash's obfuscated
 * `com.sulake.habbo.communication.enum.§_-T1k§` (paired in `scripts/drift/known.py`).
 */
export enum ClientDeviceCategoryEnum {
    Unknown = 0,
    Phone = 1,
    Tablet = 2,
    Browser = 3,
    Desktop = 4,
}
