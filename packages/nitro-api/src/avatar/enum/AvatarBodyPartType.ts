export enum AvatarBodyPartType {
    Top = 'top',
    Bottom = 'bottom',
    Behind = 'behind',
    Torso = 'torso',
    LeftItem = 'leftitem',
    RightItem = 'rightitem',
    LeftArm = 'leftarm',
    RightArm = 'rightarm',
    /** The pet held in the left hand; ordered before the arm so the hand draws over it. */
    PetLeft = 'petl',
    PetRight = 'petr',
    Head = 'head',
}
