/**
 * `AvatarAction.GESTURE_MAP` - the index a gesture is named by on the wire and in
 * `updateRoomObjectUserGesture`. The map's own order is what gives each its number.
 */
export enum AvatarGestureType {
    None = 0,
    Smile = 1,
    Angry = 2,
    Surprised = 3,
    Sad = 4,
    Joy = 5,
    Crazy = 6,
    Tongue = 7,
    EyeBlink = 8,
    Mischievous = 9,
    Puzzled = 10,
}
