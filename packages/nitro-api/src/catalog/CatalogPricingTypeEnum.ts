/** Flash's `Offer.PRICE_TYPE_*`, as an int enum (see `known.py`). */
export enum CatalogPricingTypeEnum {
    None = 0,
    Credits = 1,
    ActivityPoints = 2,
    CreditsActivityPoints = 3,
    Silver = 4,
}
